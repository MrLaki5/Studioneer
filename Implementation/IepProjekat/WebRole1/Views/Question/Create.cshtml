@{
    ViewBag.Title = "Write Question";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .btn-file {
        position: relative;
        overflow: hidden;
    }

        .btn-file input[type=file] {
            position: absolute;
            top: 0;
            right: 0;
            min-width: 100%;
            min-height: 100%;
            font-size: 100px;
            text-align: right;
            filter: alpha(opacity=0);
            opacity: 0;
            outline: none;
            background: white;
            cursor: inherit;
            display: block;
        }

    #img-upload {
        width: 200px;
    }
</style>

<h2>WriteQuestion</h2>

@using (Html.BeginForm("Create", "Question", FormMethod.Post, new { enctype = "multipart/form-data", id ="FormQ", role="form" }))
{
    @Html.AntiForgeryToken()


    @Html.ValidationSummary("", new { @class = "text-danger center" })

    <div class="form-group">
        @Html.Label("Title:")
        @Html.TextBox("Title", null, new { @class = "form-control", @name = "title",@maxlength="20" })
    </div>

    <div class="form-group">
        <label for="text">Question Text:</label>
        <textarea class="form-control" rows="5" id="text" maxlength="200" required></textarea>
    </div>


            <div class="form-group">
                <label>Upload Image</label>
                <div class="input-group">
                    <span class="input-group-btn">
                        <span class="btn btn-default btn-file">
                            Browse… <input type="file" id="imgInp" name="file">
                        </span>
                    </span>
                    <input type="text" class="form-control" readonly>
                </div>
                <img id='img-upload' />
            </div>


                var incrementor = 64;
                

                for (var i = 0; i < ViewBag.k; i++)
                {
                    incrementor++;
                   var incrementorA = (char)incrementor;
                    <div class="input-group">
            <span class="input-group-addon">@incrementorA.)</span>
            <input id="msg" type="text" class="form-control" name="answer @i" placeholder="Additional Info" maxlength="100" required>
            &nbsp<label class=".radio-inline"><input type="radio" name="radio202">Select Correct Answer</label>
        </div>
    }
    <div class="form-group"></div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary" id="butt1"><span class="glyphicon glyphicon-print"></span>&nbsp Save</button>
    </div>

}
<script src="~/Scripts/jquery-1.10.2.js" type="text/javascript"></script>
<script type="text/javascript">

    $(document).ready(function () {

      /*  $("#FormQ").validate({
            rules: {
                Title: {
                    required: true,
                    maxlength: 20
                },
                text: {
                    required: true,
                    maxlength: 200
                },
                msg: {
                    required: true,
                    maxlength: 100
                }
            },
            messages: { 
                title: {
                    required: "We need your email address to contact you",
                    maxlength: "Your email address must be in the format of name@domain.com"
                },
                text: {
                    required: "We need your email address to contact you",
                    maxlength: "Your email address must be in the format of name@domain.com"
                },
                msg: {
                    required: "We need your email address to contact you",
                    maxlength: "Your email address must be in the format of name@domain.com"
                }
            }
        });

        */
        $(document).on('change', '.btn-file :file', function () {
            var input = $(this),
                label = input.val().replace(/\\/g, '/').replace(/.*\//, '');
            input.trigger('fileselect', [label]);
        });

        $('.btn-file :file').on('fileselect', function (event, label) {

            var input = $(this).parents('.input-group').find(':text'),
                log = label;

            if (input.length) {
                input.val(log);
            } else {
                if (log) alert(log);
            }

        });
        function readURL(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();

                reader.onload = function (e) {
                    $('#img-upload').attr('src', e.target.result);
                }

                reader.readAsDataURL(input.files[0]);
            }
        }

        $("#imgInp").change(function () {
            readURL(this);
        });
    });
</script>